import React from 'react'
import Hero from '../Hero/logo.png'

const hero = (props) => (
   <div className="HeroContainer">
    <div className="HeroImg">
        <img class="HeroLogo" src={Hero} alt="El Lagunero Image"/>
    </div>
    </div>
);

export default hero;