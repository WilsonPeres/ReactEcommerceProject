import React from 'react';
import Logo from '../Logo/logo.png'
const logo = (props) => (
   
    <div className="NavLogo">
        <img src={Logo} alt="El Lagunero Image"/>
    </div>
);

export default logo;